// Package version provides the project version used by binaries and release builds.
package version

// Current is the regiondb project version. GoReleaser replaces it with the
// release version derived from the tag.
var Current = "1.1.0"
